package main

import ( // fmt for printing
	"fmt"
)

func main() {
	fmt.Println("Hello World!")
}

//need to compile code first in go
//later run the code

//if nothing happens, then the compiler worked.

//some notes about windows. 'ls' = 'dir', './hello' = 'hello.exe'
//the former shown is linux / macos and the latter is windows command prompt
//everything shown DOES work in vscode terminal, however, which is pretty nice

// 'hello' is PERMANENTLY STUCK UAWEFGIOWEJFO ugh